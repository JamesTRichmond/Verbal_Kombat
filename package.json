{
  "name": "verbal-kombat",
  "version": "0.1.0",
  "private": true,
  "description": "Mortal Kombat-style debate decider: facts land as hits, fallacies get blocked",
  "scripts": {
    "dev": "node --env-file-if-exists=.env server/index.js",
    "start": "node server/index.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.3",
    "express": "^5.2.1"
  }
}
